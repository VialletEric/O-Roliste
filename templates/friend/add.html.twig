{% extends 'base.html.twig' %}

{% block body %}

<h1>Ajouter un ami</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-danger">Ajouter</button>
{{ form_end(form) }}


{% endblock %}